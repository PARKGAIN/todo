import Checkbox from "../Checkbox";

const UpdateTodoForm = ({}) => {
  return (
    <div>
      <Checkbox id={} isCompleted={} setTodos={} text={} images={} />
      <input />
      <button>제출</button>
      <button>취소</button>
    </div>
  );
};

export default UpdateTodoForm;
