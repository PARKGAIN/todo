import Modal from "../Modal";

const UpdateTodoModal = () => {
  return (
    <Modal
      show={false}
      onCloseModal={() => {
        ("");
      }}
    >
      <div></div>
    </Modal>
  );
};

export default UpdateTodoModal;
